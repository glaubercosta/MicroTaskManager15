# GC-b — Auth (login, sessão, proteção de rota) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Autenticação single-user: Supabase local rodando, clientes SSR (`@supabase/ssr`), middleware de refresh de sessão, proteção de rota, tela de login (e-mail/senha), logout; dono provisionado por seed. Cobre o RF-1 do PRD.

**Architecture:** `@supabase/ssr` com cliente de browser (singleton) e cliente de servidor (por request, cookies async do Next 16). Um middleware faz refresh da sessão e redireciona não-autenticados para `/login`. A **decisão de redirect** é extraída para uma função pura testável (`shouldRedirectToLogin`), pois middleware/Server Components async não são testáveis em unit (limitação do Vitest). Login via Server Action.

**Tech Stack:** Next.js 16.2.10, `@supabase/supabase-js`, `@supabase/ssr`, Supabase CLI via `npx supabase`, Vitest.

> **Base:** implementa a story **GC-b** do design [`2026-07-18-microtaskmanager-v1-design.md`](../specs/2026-07-18-microtaskmanager-v1-design.md) (§4). Depende da GC-a (scaffolding + CI), já na `main`.
>
> **Convenções de ambiente (decididas 2026-07-18):**
> - Supabase CLI **não está no PATH** → sempre use **`npx supabase ...`**.
> - Já existe um stack Supabase de **outro projeto (V14)** rodando nas **portas padrão** (54321+). O MTM15 deve usar **portas próprias** no `supabase/config.toml` para coexistir. NÃO pare o stack do V14.
> - Regra de versão (PRD §9): confirme as APIs do `@supabase/ssr` na versão instalada antes de codar; se divergir do código abaixo, siga a versão instalada e reporte.
> - **Segredos:** `.env.local` NUNCA é commitado (o `.gitignore` do Next já ignora `.env*`). As chaves locais do Supabase são de desenvolvimento (não são segredos reais), mas mesmo assim não commite `.env.local`.

---

## File Structure (ao fim da GC-b)

- Create: `supabase/config.toml` (via `npx supabase init`, com portas ajustadas), `supabase/seed.sql`
- Create: `.env.local` (NÃO commitado) e `.env.example` (commitado, sem valores reais)
- Create: `src/lib/supabase/client.ts` — cliente de browser
- Create: `src/lib/supabase/server.ts` — cliente de servidor (cookies async)
- Create: `src/lib/supabase/middleware.ts` — `updateSession` (refresh + redirect)
- Create: `middleware.ts` (raiz) — entrypoint do middleware Next
- Create: `src/lib/auth/route-guard.ts` — `shouldRedirectToLogin` (pura, testável)
- Test: `src/lib/auth/route-guard.test.ts`
- Create: `src/app/login/page.tsx` — tela de login
- Create: `src/app/login/actions.ts` — Server Actions `signIn` / `signOut`
- Create: `src/lib/auth/credentials.ts` — validação pura de credenciais (`validateCredentials`)
- Test: `src/lib/auth/credentials.test.ts`
- Modify: `src/app/page.tsx` — passa a exigir sessão (mostra e-mail do dono + botão sair)

---

## Task 1: Supabase local em portas próprias + dependências

- [ ] **Step 1: Instalar SDKs**

Run: `npm install @supabase/supabase-js @supabase/ssr`
Expected: adicionados em `dependencies`.

- [ ] **Step 2: Inicializar o Supabase no projeto**

Run: `npx supabase init`
Expected: cria `supabase/config.toml` e `supabase/.gitignore`. Se perguntar sobre gerar settings do VS Code/Deno, responda **No**.

- [ ] **Step 3: Ajustar as portas no `supabase/config.toml` para não colidir com o V14**

Edite `supabase/config.toml` e troque as portas padrão pelas do bloco abaixo (offset +1000). Confirme que `project_id = "MicroTaskManager15"` (default vindo da pasta).
```toml
[api]
port = 55321
[db]
port = 55322
[db.pooler]
port = 55329
[studio]
port = 55323
[inbucket]
port = 55324
[analytics]
port = 55327
```
> Se a versão do CLI tiver seções/portas adicionais (ex.: `[realtime]`, `[storage]`), ajuste também qualquer porta que ainda esteja no range 54321–543xx para o range 553xx. O objetivo: nenhuma porta do MTM15 pode coincidir com uma porta em uso pelo V14.

- [ ] **Step 4: Subir o stack**

Run: `npx supabase start`
Expected: sobe os containers `supabase_*_MicroTaskManager15` e imprime `API URL: http://127.0.0.1:55321`, `Studio URL: http://127.0.0.1:55323`, e as chaves (`anon key` / `publishable key`). **Anote a API URL e a publishable/anon key.** Se falhar por porta em uso, ajuste a porta conflitante no config.toml e rode `npx supabase stop` seguido de `npx supabase start`.

- [ ] **Step 5: Variáveis de ambiente**

Create `.env.local` (NÃO commitar) com os valores impressos no Step 4:
```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:55321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<publishable ou anon key do Step 4>
```
Create `.env.example` (commitável), sem valores reais:
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
```
Confirme que `.gitignore` ignora `.env*` (o bloco do Next já faz isso) — MAS garanta que `.env.example` NÃO seja ignorado (adicione `!.env.example` ao `.gitignore` se necessário).

- [ ] **Step 6: Verificar e commitar (sem segredos)**

Run: `curl -s http://127.0.0.1:55321/rest/v1/ -H "apikey: <key>" -o /dev/null -w "%{http_code}\n"` → espera `200` (ou `401`/`404` — qualquer resposta HTTP prova que a API subiu).
Run: `git status --short` → confirme que `.env.local` NÃO aparece para commit.
```bash
git add supabase/config.toml supabase/.gitignore .env.example .gitignore package.json package-lock.json
git commit -m "GC-10 Supabase local (portas proprias) + SDKs + env example"
```
> NÃO comite `supabase/.temp`, `.env.local`, nem `.branches`. O `supabase init` já cria um `supabase/.gitignore` cobrindo isso — confirme.

---

## Task 2: Clientes Supabase (browser + server)

- [ ] **Step 1: Cliente de browser**

Create `src/lib/supabase/client.ts`:
```ts
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
  )
}
```

- [ ] **Step 2: Cliente de servidor (cookies async do Next 16)**

Create `src/lib/supabase/server.ts`:
```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options),
            )
          } catch {
            // Chamado de um Server Component — o middleware cuida do refresh.
          }
        },
      },
    },
  )
}
```

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: exit 0.

- [ ] **Step 4: Commit**
```bash
git add src/lib/supabase/client.ts src/lib/supabase/server.ts
git commit -m "GC-10 clientes Supabase browser e server (SSR)"
```

---

## Task 3: Middleware (refresh de sessão + proteção de rota) com decisão pura testável

- [ ] **Step 1: Escrever o teste da decisão de redirect (falha primeiro)**

Create `src/lib/auth/route-guard.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { shouldRedirectToLogin } from './route-guard'

describe('shouldRedirectToLogin', () => {
  it('redireciona quando não há usuário e a rota não é pública', () => {
    expect(shouldRedirectToLogin({ hasUser: false, pathname: '/' })).toBe(true)
  })

  it('não redireciona quando não há usuário mas a rota é /login', () => {
    expect(shouldRedirectToLogin({ hasUser: false, pathname: '/login' })).toBe(false)
  })

  it('não redireciona quando há usuário', () => {
    expect(shouldRedirectToLogin({ hasUser: true, pathname: '/' })).toBe(false)
  })

  it('trata sub-rotas de /login como públicas', () => {
    expect(shouldRedirectToLogin({ hasUser: false, pathname: '/login/reset' })).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npm run test -- --run src/lib/auth/route-guard.test.ts`
Expected: FAIL (módulo/função inexistente).

- [ ] **Step 3: Implementar a decisão pura**

Create `src/lib/auth/route-guard.ts`:
```ts
const PUBLIC_PREFIXES = ['/login']

export function shouldRedirectToLogin(input: {
  hasUser: boolean
  pathname: string
}): boolean {
  if (input.hasUser) return false
  return !PUBLIC_PREFIXES.some((prefix) => input.pathname.startsWith(prefix))
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npm run test -- --run src/lib/auth/route-guard.test.ts`
Expected: PASS (4 testes).

- [ ] **Step 5: Implementar `updateSession` (usa a decisão pura)**

Create `src/lib/supabase/middleware.ts`:
```ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'
import { shouldRedirectToLogin } from '@/lib/auth/route-guard'

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value),
          )
          response = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            response.cookies.set(name, value, options),
          )
        },
      },
    },
  )

  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (shouldRedirectToLogin({ hasUser: !!user, pathname: request.nextUrl.pathname })) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  return response
}
```
> Nota: o código canônico do `@supabase/ssr` usa `getUser()` no middleware. Se a doc da versão instalada recomendar `getClaims()`, adapte trocando a chamada (o `!!user` vira presença de claims) e reporte.

- [ ] **Step 6: Entrypoint do middleware**

Create `middleware.ts` (na raiz do projeto):
```ts
import { type NextRequest } from 'next/server'
import { updateSession } from '@/lib/supabase/middleware'

export async function middleware(request: NextRequest) {
  return await updateSession(request)
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

- [ ] **Step 7: Typecheck + suíte**

Run: `npm run typecheck && npm run test -- --run`
Expected: exit 0; testes passam.

- [ ] **Step 8: Commit**
```bash
git add src/lib/auth/route-guard.ts src/lib/auth/route-guard.test.ts src/lib/supabase/middleware.ts middleware.ts
git commit -m "GC-10 middleware de sessao + protecao de rota (decisao pura testada)"
```

---

## Task 4: Login (Server Action) + validação pura + logout

- [ ] **Step 1: Teste da validação de credenciais (falha primeiro)**

Create `src/lib/auth/credentials.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { validateCredentials } from './credentials'

describe('validateCredentials', () => {
  it('aceita e-mail e senha válidos', () => {
    expect(validateCredentials('dono@exemplo.com', 'segredo123')).toEqual({ ok: true })
  })

  it('rejeita e-mail vazio', () => {
    const r = validateCredentials('  ', 'segredo123')
    expect(r.ok).toBe(false)
  })

  it('rejeita e-mail sem @', () => {
    expect(validateCredentials('donoexemplo.com', 'segredo123').ok).toBe(false)
  })

  it('rejeita senha vazia', () => {
    expect(validateCredentials('dono@exemplo.com', '').ok).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npm run test -- --run src/lib/auth/credentials.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar a validação pura**

Create `src/lib/auth/credentials.ts`:
```ts
export type CredentialsCheck = { ok: true } | { ok: false; error: string }

export function validateCredentials(email: string, password: string): CredentialsCheck {
  const e = email.trim()
  if (e.length === 0) return { ok: false, error: 'Informe o e-mail.' }
  if (!e.includes('@')) return { ok: false, error: 'E-mail inválido.' }
  if (password.length === 0) return { ok: false, error: 'Informe a senha.' }
  return { ok: true }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npm run test -- --run src/lib/auth/credentials.test.ts`
Expected: PASS (4 testes).

- [ ] **Step 5: Server Actions de login/logout**

Create `src/app/login/actions.ts`:
```ts
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { validateCredentials } from '@/lib/auth/credentials'

export async function signIn(_prev: unknown, formData: FormData) {
  const email = String(formData.get('email') ?? '')
  const password = String(formData.get('password') ?? '')

  const check = validateCredentials(email, password)
  if (!check.ok) return { error: check.error }

  const supabase = await createClient()
  const { error } = await supabase.auth.signInWithPassword({
    email: email.trim(),
    password,
  })
  if (error) return { error: 'E-mail ou senha incorretos.' }

  redirect('/')
}

export async function signOut() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  redirect('/login')
}
```

- [ ] **Step 6: Tela de login**

Create `src/app/login/page.tsx`:
```tsx
'use client'

import { useActionState } from 'react'
import { signIn } from './actions'

export default function LoginPage() {
  const [state, formAction, pending] = useActionState(signIn, null)

  return (
    <main style={{ maxWidth: 360, margin: '10vh auto', padding: 24 }}>
      <h1>Entrar</h1>
      <form action={formAction}>
        <label>
          E-mail
          <input name="email" type="email" autoComplete="email" required />
        </label>
        <label>
          Senha
          <input name="password" type="password" autoComplete="current-password" required />
        </label>
        {state?.error ? <p role="alert">{state.error}</p> : null}
        <button type="submit" disabled={pending}>
          {pending ? 'Entrando…' : 'Entrar'}
        </button>
      </form>
    </main>
  )
}
```
> A estética real (tokens, layout) vem na GC-f; aqui é funcional e sóbrio. Se `useActionState` não existir na versão do React instalada, confirme a API (React 19 exporta `useActionState` de `react`).

- [ ] **Step 7: Home protegida com logout**

Replace `src/app/page.tsx` com:
```tsx
import { createClient } from '@/lib/supabase/server'
import { signOut } from './login/actions'

export default async function Home() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  return (
    <main style={{ maxWidth: 600, margin: '6vh auto', padding: 24 }}>
      <h1>MicroTaskManager</h1>
      <p>Sessão de {user?.email ?? 'desconhecido'}.</p>
      <form action={signOut}>
        <button type="submit">Sair</button>
      </form>
    </main>
  )
}
```
> O middleware já garante que sem sessão o usuário é mandado a `/login` antes de chegar aqui.

- [ ] **Step 8: Typecheck + lint + suíte + build**

Run: `npm run typecheck && npm run lint && npm run test -- --run && npm run build`
Expected: todos exit 0. (O build precisa das envs — garanta que `.env.local` existe. Se o build falhar por falta de env em CI, note: o CI NÃO roda build, só typecheck/lint/test.)

- [ ] **Step 9: Commit**
```bash
git add src/lib/auth/credentials.ts src/lib/auth/credentials.test.ts src/app/login/ src/app/page.tsx
git commit -m "GC-10 login (server action) + validacao pura + logout + home protegida"
```

---

## Task 5: Seed do dono + verificação manual de login/logout

- [ ] **Step 1: Seed do usuário dono**

Create `supabase/seed.sql` (roda em `supabase db reset` / no start). Cria o dono se não existir:
```sql
-- Usuário dono (single-user). Ajuste e-mail/senha conforme desejado.
-- Usa a função interna de signup do GoTrue via inserção direta é frágil;
-- o caminho suportado é criar via Admin API. Para o seed local, documentamos o
-- comando abaixo (Step 2). Este arquivo fica como âncora e para dados futuros.
```
> Observação honesta: criar usuário do Auth por SQL puro é frágil (hash de senha, colunas internas do GoTrue). O caminho suportado é a Admin API / Studio. Portanto o "seed" real do dono é o Step 2, documentado; `seed.sql` fica reservado para dados de domínio (listas/tarefas) em stories futuras.

- [ ] **Step 2: Criar o dono via Admin API (documentado + executado)**

Run (substitua a `service_role key` impressa no `npx supabase start`, e escolha e-mail/senha):
```bash
curl -s -X POST 'http://127.0.0.1:55321/auth/v1/admin/users' \
  -H "apikey: <SERVICE_ROLE_KEY>" \
  -H "Authorization: Bearer <SERVICE_ROLE_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"email":"dono@exemplo.com","password":"segredo123","email_confirm":true}'
```
Expected: JSON com o `id` do usuário criado. Registre o e-mail/senha usados no `AGENTS.md` (seção "Conta de desenvolvimento") — são credenciais locais de dev, não segredo de produção.

- [ ] **Step 3: Verificação manual (evidência de DoD)**

Run: `npm run dev` (deixe rodando), abra `http://localhost:3000`:
1. Sem sessão → deve redirecionar para `/login`.
2. Logar com credencial errada → mensagem "E-mail ou senha incorretos.".
3. Logar com o dono → cai na home mostrando o e-mail.
4. Sair → volta para `/login`.
Pare o dev server (Ctrl+C). Anote o resultado no report.

- [ ] **Step 4: Documentar a conta de dev no AGENTS.md**

Adicione ao `AGENTS.md` uma seção:
```markdown
## Conta de desenvolvimento (Supabase local)
- Supabase local em portas próprias (ver `supabase/config.toml`); suba com `npx supabase start`.
- Dono (dev): `dono@exemplo.com` / `segredo123` (criado via Admin API — ver plano GC-b).
- `.env.local` (não versionado) aponta para a API local; use `.env.example` como molde.
```

- [ ] **Step 5: Commit**
```bash
git add supabase/seed.sql AGENTS.md
git commit -m "GC-10 seed/documenta conta do dono e verificacao de login"
```

---

## Self-Review (preenchido)

**Spec coverage (design §4 GC-b / PRD RF-1):** Supabase local (T1) ✓; sessão SSR com `@supabase/ssr` (T2, T3) ✓; login e-mail/senha (T4, RF-1.2) ✓; login inválido → mensagem (T4/T5, RF-1.3) ✓; proteção de rota / redirect sem sessão (T3, RF-1.1) ✓; sessão válida → app (T3/T4) ✓; logout (T4, RF-1.4) ✓; dono por seed, sem cadastro no app (T5) ✓. DoD "logar/deslogar; rota protegida redireciona; teste de proteção e login inválido": testes puros `route-guard`/`credentials` (T3/T4) + verificação manual (T5) ✓.

**Placeholder scan:** sem TBD; todo step tem comando/código. Incertezas sinalizadas e cobertas por "regra de versão" (getUser vs getClaims; useActionState).

**Type consistency:** `createClient()` (server, async) usado em actions e page; `shouldRedirectToLogin({hasUser,pathname})` idêntico em teste, guard e middleware; `validateCredentials(email,password)` idêntico em teste, guard e action; `CredentialsCheck` union usado de forma consistente.

---

## Nota de fluxo
Branch `glaubercosta/gc-<id>-...` (o ID real da issue GC-b será atribuído na criação — provável `GC-10`); PR `Fixes GC-<id>`. Não referenciar (`Ref`) issues concluídas. CI (typecheck+lint+test) precisa passar; o build NÃO roda no CI (evita depender de env/credenciais).
