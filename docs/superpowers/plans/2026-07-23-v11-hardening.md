# v1.1 Hardening (GC-18) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Endurecer o v1 do MicroTaskManager em acessibilidade (contraste do PriorityDot), segurança (cookie de tema, posse de `list_id`, escopo do logout), performance (filtro por lista no banco + índice) e robustez da suíte de testes — 7 issues (GC-19 → GC-25), cada uma com branch/PR/merge próprios.

**Architecture:** Next.js 16 (App Router, server actions) + Supabase (RLS como fronteira de segurança). Funções puras em `src/domain/`; acesso a dados em `src/lib/supabase/`. Testes: vitest unit (jsdom), vitest integration (Supabase local, config separada), Playwright E2E (conta única, serial). Integration/E2E ficam **fora** do gate `ci` do GitHub (convenção do v1).

**Tech Stack:** TypeScript, React 19, Next 16.2, @supabase/ssr, vitest 4, Playwright 1.61, Supabase CLI, gh CLI.

---

## Processo obrigatório por issue (repetido em cada task)

**Linear é a fonte de verdade.** Cada task deste plano é uma issue GC e segue o ciclo:

1. Partir da `main` atualizada e criar a branch **exata** gerada pelo Linear (informada em cada task).
2. TDD nos passos da task; commits com prefixo `GC-<n>` (padrão do repo, ex.: `GC-19 tokens sólidos…`).
3. Push + PR com **ID no título** e **`Fixes GC-<n>` no corpo** (nunca abrir PR sem o ID — quebra o rastreamento).
4. Aguardar o check `ci` verde e mergear (merge commit, padrão do repo). O Linear move a issue sozinho (PR aberto → In Progress; merge → Done). Não mexer manualmente no status.
5. Só iniciar a task seguinte depois do merge (GC-23 e GC-25 tocam os mesmos specs E2E; a serialização evita conflito).

**Pré-requisitos do ambiente (verificar uma vez antes da Task 1):**

- Supabase local no ar: `npx supabase status` (se parado: `npx supabase start`).
- `.env.local` com `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `E2E_OWNER_EMAIL`, `E2E_OWNER_PASSWORD`.
- `gh auth status` OK.
- Comandos de teste: unit `npx vitest --run`, integration `npm run test:integration`, E2E `npm run e2e` (sobe o dev server sozinho na porta 3100).

---

### Task 1: GC-19 · Contraste do PriorityDot `low` (WCAG 1.4.11)

O dot de prioridade é monocromático por **opacidade** sobre `--foreground` ([globals.css:119-130](../../../src/app/globals.css)); no tema claro, `low` (opacity 0.3) fica abaixo de 3:1 contra o fundo. Trocar por **cores sólidas por token** (`--dot-high/medium/low`) por tema, todas ≥3:1 contra `--background`. O componente [priority-dot.tsx](../../../src/app/priority-dot.tsx) não muda (já expõe `data-priority`); só CSS + teste novo.

Valores já validados (razão de contraste vs fundo): dark `#ededf0`=16.1, `#a2a2ad`=7.5, `#6b6b76`=3.6; light `#1c1c21`=17.0, `#5c5c66`=6.6, `#85859a`=3.6. Hierarquia visual preservada (high > medium > low nos dois temas).

**Files:**
- Create: `src/app/priority-dot-contrast.test.ts`
- Modify: `src/app/globals.css`
- Este PR também carrega o spec e este plano (push direto na `main` é bloqueado): `docs/superpowers/specs/2026-07-23-v11-hardening-design.md`, `docs/superpowers/plans/2026-07-23-v11-hardening.md`.

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-19-contraste-do-prioritydot-low-no-tema-claro-wcag-1411
```

- [ ] **Step 2: Commitar spec + plano (entram no repo por esta branch)**

```bash
git add docs/superpowers/specs/2026-07-23-v11-hardening-design.md docs/superpowers/plans/2026-07-23-v11-hardening.md
git commit -m "GC-18 spec e plano do v1.1 hardening"
```

- [ ] **Step 3: Escrever o teste de contraste (falha primeiro)**

Criar `src/app/priority-dot-contrast.test.ts`. A utilidade WCAG vive **no teste** (não é código de produto). Os valores são lidos direto de `globals.css` por regex — mudar o CSS sem manter contraste quebra o teste, sem constante duplicada.

```ts
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import { join, dirname } from 'node:path'
import { fileURLToPath } from 'node:url'

/**
 * GC-19 (WCAG 1.4.11): os tokens --dot-* devem ter contraste ≥3:1 contra o
 * fundo do respectivo tema, lidos direto de globals.css.
 */
const css = readFileSync(
  join(dirname(fileURLToPath(import.meta.url)), 'globals.css'),
  'utf8',
)

/** Luminância relativa WCAG de uma cor #rrggbb. */
function relativeLuminance(hex: string): number {
  const channels = [1, 3, 5].map((i) => parseInt(hex.slice(i, i + 2), 16) / 255)
  const [r, g, b] = channels.map((c) =>
    c <= 0.03928 ? c / 12.92 : ((c + 0.055) / 1.055) ** 2.4,
  )
  return 0.2126 * r + 0.7152 * g + 0.0722 * b
}

/** Razão de contraste WCAG entre duas cores #rrggbb. */
function contrastRatio(a: string, b: string): number {
  const [hi, lo] = [relativeLuminance(a), relativeLuminance(b)].sort((x, y) => y - x)
  return (hi + 0.05) / (lo + 0.05)
}

function themeBlock(theme: 'dark' | 'light'): string {
  const re =
    theme === 'dark' ? /:root \{([^}]*)\}/ : /:root\[data-theme='light'\] \{([^}]*)\}/
  const m = css.match(re)
  if (!m) throw new Error(`bloco do tema ${theme} não encontrado em globals.css`)
  return m[1]
}

function token(block: string, name: string): string {
  const m = block.match(new RegExp(`${name}:\\s*(#[0-9a-fA-F]{6})`))
  if (!m) throw new Error(`token ${name} não encontrado`)
  return m[1]
}

const PRIORITIES = ['high', 'medium', 'low'] as const

describe.each(['dark', 'light'] as const)('tokens --dot-* no tema %s (GC-19)', (theme) => {
  const block = themeBlock(theme)
  const background = token(block, '--background')

  it.each([...PRIORITIES])('--dot-%s tem contraste ≥3:1 contra o fundo', (p) => {
    expect(contrastRatio(token(block, `--dot-${p}`), background)).toBeGreaterThanOrEqual(3)
  })

  it('preserva a hierarquia visual: high > medium > low em contraste', () => {
    const [high, medium, low] = PRIORITIES.map((p) =>
      contrastRatio(token(block, `--dot-${p}`), background),
    )
    expect(high).toBeGreaterThan(medium)
    expect(medium).toBeGreaterThan(low)
  })
})

it('o dot não usa mais peso por opacidade (cores sólidas por token)', () => {
  expect(css).not.toMatch(/\.priority-dot\[data-priority[^}]*opacity/s)
})
```

- [ ] **Step 4: Rodar e ver falhar**

Run: `npx vitest --run src/app/priority-dot-contrast.test.ts`
Expected: FAIL — `token --dot-high não encontrado`.

- [ ] **Step 5: Adicionar tokens e trocar opacidade por cor em `globals.css`**

No bloco `:root {` (tema dark), depois de `--due-today: #e2b04a;` adicionar:

```css
  --dot-high: #ededf0;
  --dot-medium: #a2a2ad;
  --dot-low: #6b6b76;
```

No bloco `:root[data-theme='light'] {`, depois de `--due-today: #8a5a00;` adicionar:

```css
  --dot-high: #1c1c21;
  --dot-medium: #5c5c66;
  --dot-low: #85859a;
```

Substituir o bloco inteiro do dot (linhas 119–130: o comentário `/* Pontinho de prioridade monocromático... */`, a regra `.priority-dot` e as duas regras de opacity) por:

```css
/* Pontinho de prioridade: cor sólida por token, ≥3:1 contra o fundo (GC-19 / WCAG 1.4.11). */
.priority-dot[data-priority='high'] {
  color: var(--dot-high);
}

.priority-dot[data-priority='medium'] {
  color: var(--dot-medium);
}

.priority-dot[data-priority='low'] {
  color: var(--dot-low);
}
```

- [ ] **Step 6: Rodar testes e gates**

Run: `npx vitest --run` — Expected: PASS (inclui `priority-dot.test.tsx` existente, que não depende de opacity).
Run: `npm run lint && npm run typecheck` — Expected: sem erros.

- [ ] **Step 7: Commit**

```bash
git add src/app/globals.css src/app/priority-dot-contrast.test.ts
git commit -m "GC-19 dot de prioridade com tokens sólidos ≥3:1 (WCAG 1.4.11)"
```

- [ ] **Step 8: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-19-contraste-do-prioritydot-low-no-tema-claro-wcag-1411
gh pr create --title "GC-19 Contraste do PriorityDot low no tema claro (WCAG 1.4.11)" --body "Troca opacidade por tokens sólidos --dot-* (≥3:1 contra o fundo nos dois temas), com teste de contraste lendo os valores direto do globals.css. Carrega também o spec e o plano do v1.1.

Fixes GC-19

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

Nota (risco do spec): a validação "a olho" da hierarquia nos dois temas fica para o review humano do PR; o teste de hierarquia por contraste cobre o objetivo automatizável.

---

### Task 2: GC-20 · Cookie de tema: `Secure` + parsing exato

Dois problemas em [theme-toggle.tsx:30](../../../src/app/theme-toggle.tsx) e no script inline de [layout.tsx:26](../../../src/app/layout.tsx): (a) cookie `theme` sem `Secure`; (b) o script detecta o cookie com `indexOf('theme=')`, que casa `x-theme=` também.

Solução: funções puras `hasThemeCookie` e `themeCookie` em `src/domain/theme.ts`; o toggle usa `themeCookie(next, location.protocol === 'https:')` (dev local via HTTP continua sem Secure); o script inline **replica** a lógica (é string, não importa módulo) e sai do layout para um módulo próprio testável. `SameSite=Lax` mantido e conferido nos dois caminhos. *(Adaptação do spec: não existe server action de toggle — o toggle é client-side; o condicional é `location.protocol`, equivalente ao objetivo "Secure só em produção/HTTPS".)*

**Files:**
- Modify: `src/domain/theme.ts`, `src/domain/theme.test.ts`
- Create: `src/app/theme-script.ts`, `src/app/theme-script.test.ts`
- Modify: `src/app/layout.tsx`, `src/app/theme-toggle.tsx`

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-20-endurecer-cookie-de-tema-flag-secure-parsing-exato-no-script
```

- [ ] **Step 2: Testes das funções puras (falham primeiro)**

Acrescentar ao final de `src/domain/theme.test.ts` (e adicionar `hasThemeCookie, themeCookie` ao import de `'./theme'`):

```ts
describe('hasThemeCookie (GC-20)', () => {
  it('string vazia → false', () => {
    expect(hasThemeCookie('')).toBe(false)
  })
  it('só theme= → true', () => {
    expect(hasThemeCookie('theme=dark')).toBe(true)
    expect(hasThemeCookie('theme=')).toBe(true)
  })
  it('x-theme= sem theme= → false (prefixo exato, não substring)', () => {
    expect(hasThemeCookie('x-theme=light')).toBe(false)
  })
  it('x-theme= e theme= juntos → true', () => {
    expect(hasThemeCookie('x-theme=light; theme=dark')).toBe(true)
  })
})

describe('themeCookie (GC-20)', () => {
  it('sob HTTP não emite Secure e mantém SameSite=Lax', () => {
    expect(themeCookie('dark', false)).toBe(
      'theme=dark; path=/; max-age=31536000; samesite=lax',
    )
  })
  it('sob HTTPS anexa Secure (e mantém SameSite=Lax)', () => {
    const c = themeCookie('light', true)
    expect(c).toContain('samesite=lax')
    expect(c.endsWith('; secure')).toBe(true)
  })
})
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `npx vitest --run src/domain/theme.test.ts`
Expected: FAIL — `hasThemeCookie` não exportado.

- [ ] **Step 4: Implementar em `src/domain/theme.ts`**

Acrescentar ao final do arquivo:

```ts
/** Detecção exata do cookie de tema num document.cookie: 'x-theme=' não casa (GC-20). */
export function hasThemeCookie(cookieString: string): boolean {
  return cookieString.split('; ').some((c) => c.startsWith(`${THEME_COOKIE}=`))
}

/** Serializa o cookie de tema; `Secure` só sob HTTPS — dev local via HTTP segue gravando (GC-20). */
export function themeCookie(theme: Theme, isHttps: boolean): string {
  const base = `${THEME_COOKIE}=${theme}; path=/; max-age=${THEME_COOKIE_MAX_AGE}; samesite=lax`
  return isHttps ? `${base}; secure` : base
}
```

Run: `npx vitest --run src/domain/theme.test.ts` — Expected: PASS.

- [ ] **Step 5: Teste do script inline (falha primeiro)**

Criar `src/app/theme-script.test.ts`. Além das asserções estáticas (parsing novo, sem `indexOf`, Secure condicional), os testes **executam** o script em jsdom:

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { themeFallbackScript } from './theme-script'

function clearCookie(name: string) {
  document.cookie = `${name}=; path=/; max-age=0`
}

describe('themeFallbackScript (GC-20)', () => {
  beforeEach(() => {
    clearCookie('theme')
    clearCookie('x-theme')
    delete document.documentElement.dataset.theme
    localStorage.clear()
  })

  it('sem cookie: aplica o tema do localStorage e regrava o cookie', () => {
    localStorage.setItem('theme', 'light')
    eval(themeFallbackScript)
    expect(document.documentElement.dataset.theme).toBe('light')
    expect(document.cookie).toContain('theme=light')
  })

  it("'x-theme=' não conta como cookie de tema (parsing exato)", () => {
    document.cookie = 'x-theme=dark; path=/'
    localStorage.setItem('theme', 'light')
    eval(themeFallbackScript)
    expect(document.documentElement.dataset.theme).toBe('light')
  })

  it('com cookie de tema presente, não faz nada', () => {
    document.cookie = 'theme=dark; path=/'
    localStorage.setItem('theme', 'light')
    eval(themeFallbackScript)
    expect(document.documentElement.dataset.theme).toBeUndefined()
  })

  it('valor inválido no localStorage é ignorado', () => {
    localStorage.setItem('theme', 'solarized')
    eval(themeFallbackScript)
    expect(document.documentElement.dataset.theme).toBeUndefined()
  })

  it('parsing novo sem indexOf; secure condicionado a HTTPS; samesite=lax mantido', () => {
    expect(themeFallbackScript).not.toContain('indexOf')
    expect(themeFallbackScript).toContain(".startsWith('theme=')")
    expect(themeFallbackScript).toContain("location.protocol==='https:'")
    expect(themeFallbackScript).toContain('samesite=lax')
  })
})
```

Run: `npx vitest --run src/app/theme-script.test.ts` — Expected: FAIL (módulo não existe).

- [ ] **Step 6: Criar `src/app/theme-script.ts`**

```ts
import { THEME_COOKIE, THEME_COOKIE_MAX_AGE } from '@/domain/theme'

/**
 * Fallback localStorage (RF-6): roda antes do paint quando NÃO há cookie de tema.
 * É uma string inline (não importa módulo), então replica a lógica de
 * hasThemeCookie/themeCookie de src/domain/theme.ts (GC-20):
 * - detecção exata do cookie via split + startsWith ('x-theme=' não casa);
 * - `secure` só quando a página é servida via HTTPS (dev local em HTTP segue).
 */
export const themeFallbackScript =
  `(function(){try{` +
  `var has=document.cookie.split('; ').some(function(c){return c.startsWith('${THEME_COOKIE}=')});` +
  `if(!has){var t=localStorage.getItem('${THEME_COOKIE}');` +
  `if(t==='light'||t==='dark'){document.documentElement.dataset.theme=t;` +
  `var c='${THEME_COOKIE}='+t+'; path=/; max-age=${THEME_COOKIE_MAX_AGE}; samesite=lax';` +
  `if(location.protocol==='https:'){c+='; secure'}` +
  `document.cookie=c}}}catch(e){}})()`
```

Run: `npx vitest --run src/app/theme-script.test.ts` — Expected: PASS.

- [ ] **Step 7: Usar o módulo no layout e o token no toggle**

Em `src/app/layout.tsx`:
- trocar o import de tema para `import { parseTheme, THEME_COOKIE } from "@/domain/theme";` (remove `THEME_COOKIE_MAX_AGE`);
- adicionar `import { themeFallbackScript } from "./theme-script";`
- **remover** a `const themeFallbackScript = ...` local (linhas 22–26, incluindo o comentário JSDoc acima dela). O JSX não muda.

Em `src/app/theme-toggle.tsx`:
- import passa a ser:

```ts
import {
  parseTheme,
  themeCookie,
  toggleTheme,
  THEME_COOKIE,
  type Theme,
} from '@/domain/theme'
```

- em `handleClick`, trocar a linha `document.cookie = \`${THEME_COOKIE}=...\`` por:

```ts
    document.cookie = themeCookie(next, location.protocol === 'https:')
```

(`THEME_COOKIE` continua usado no `localStorage.setItem`.)

- [ ] **Step 8: Gates**

Run: `npx vitest --run` — Expected: PASS (theme-toggle.test.tsx segue verde: jsdom é http → sem secure).
Run: `npm run lint && npm run typecheck` — Expected: sem erros.
Run: `npx playwright test e2e/theme.spec.ts` — Expected: PASS (fluxo de tema intacto; precisa do Supabase local + `.env.local`).

- [ ] **Step 9: Commit**

```bash
git add src/domain/theme.ts src/domain/theme.test.ts src/app/theme-script.ts src/app/theme-script.test.ts src/app/layout.tsx src/app/theme-toggle.tsx
git commit -m "GC-20 cookie de tema com Secure condicional e parsing exato no script inline"
```

- [ ] **Step 10: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-20-endurecer-cookie-de-tema-flag-secure-parsing-exato-no-script
gh pr create --title "GC-20 Endurecer cookie de tema: flag Secure + parsing exato no script inline" --body "hasThemeCookie/themeCookie puras em src/domain/theme.ts; script inline extraído para módulo testável, com startsWith em vez de indexOf e secure sob HTTPS; SameSite=Lax conferido nos dois caminhos.

Fixes GC-20

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

---

### Task 3: GC-21 · `createTask` valida posse do `list_id`

A FK só valida existência e a RLS de `tasks` valida o `user_id` da task, não da lista — um UUID vazado permitiria criar task "dentro" de lista alheia ([actions.ts:19-47](../../../src/app/actions.ts)). Solução: helper `userOwnsList` que faz `select id` em `lists` com o client RLS do usuário; `createTask` retorna erro de validação pelo mesmo canal do form. Sem mudança de schema.

**Files:**
- Create: `src/lib/supabase/list-ownership.ts`
- Create: `src/app/actions.test.ts` (unit, mocks)
- Create: `src/lib/supabase/list-ownership.integration.test.ts` (RLS real, 2 usuários)
- Modify: `src/app/actions.ts`

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-21-createtask-validar-posse-do-list_id-recebido-do-form
```

- [ ] **Step 2: Unit test do wiring do `createTask` (falha primeiro)**

Criar `src/app/actions.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

const insert = vi.fn()

vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
vi.mock('next/navigation', () => ({ redirect: vi.fn() }))
vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(async () => ({
    auth: { getUser: vi.fn(async () => ({ data: { user: { id: 'user-1' } } })) },
    from: vi.fn(() => ({ insert })),
  })),
}))
vi.mock('@/lib/supabase/list-ownership', () => ({ userOwnsList: vi.fn() }))

import { createTask } from './actions'
import { userOwnsList } from '@/lib/supabase/list-ownership'

function form(fields: Record<string, string>): FormData {
  const fd = new FormData()
  for (const [k, v] of Object.entries(fields)) fd.set(k, v)
  return fd
}

describe('createTask valida posse do list_id (GC-21)', () => {
  beforeEach(() => {
    insert.mockReset().mockResolvedValue({ error: null })
    vi.mocked(userOwnsList).mockReset()
  })

  it('lista alheia/inexistente → erro de validação, sem insert', async () => {
    vi.mocked(userOwnsList).mockResolvedValue(false)
    const result = await createTask(null, form({ title: 'Tarefa', list_id: 'uuid-de-outro' }))
    expect(result).toEqual({ error: 'Lista inválida.' })
    expect(insert).not.toHaveBeenCalled()
  })

  it('lista própria → cria a task com o list_id', async () => {
    vi.mocked(userOwnsList).mockResolvedValue(true)
    const result = await createTask(null, form({ title: 'Tarefa', list_id: 'uuid-proprio' }))
    expect(result).toEqual({ error: null })
    expect(insert).toHaveBeenCalledWith(
      expect.objectContaining({ list_id: 'uuid-proprio', title: 'Tarefa' }),
    )
  })

  it('sem lista ("all"/vazio) → não consulta posse e cria com list_id null', async () => {
    const result = await createTask(null, form({ title: 'Tarefa', list_id: 'all' }))
    expect(result).toEqual({ error: null })
    expect(userOwnsList).not.toHaveBeenCalled()
    expect(insert).toHaveBeenCalledWith(expect.objectContaining({ list_id: null }))
  })
})
```

Run: `npx vitest --run src/app/actions.test.ts` — Expected: FAIL (`@/lib/supabase/list-ownership` não existe).

- [ ] **Step 3: Criar o helper `src/lib/supabase/list-ownership.ts`**

```ts
import type { SupabaseClient } from '@supabase/supabase-js'

/**
 * Confere, sob a RLS do usuário, se a lista existe e pertence a ele (GC-21).
 * Sem linha (lista alheia/inexistente) ou erro (id malformado) → false.
 */
export async function userOwnsList(
  supabase: SupabaseClient,
  listId: string,
): Promise<boolean> {
  const { data, error } = await supabase
    .from('lists')
    .select('id')
    .eq('id', listId)
    .maybeSingle()
  return error === null && data !== null
}
```

- [ ] **Step 4: Ligar no `createTask` em `src/app/actions.ts`**

Adicionar o import junto aos demais:

```ts
import { userOwnsList } from '@/lib/supabase/list-ownership'
```

Em `createTask`, entre `const { supabase, user } = await requireUserClient()` e o `insert`, adicionar:

```ts
  // GC-21: a FK só valida existência e a RLS de tasks só o user_id da task —
  // a posse da lista é conferida aqui, sob a RLS do usuário.
  if (list_id !== null && !(await userOwnsList(supabase, list_id))) {
    return { error: 'Lista inválida.' }
  }
```

Run: `npx vitest --run src/app/actions.test.ts` — Expected: PASS.

- [ ] **Step 5: Integration test com 2 usuários (RLS real)**

Criar `src/lib/supabase/list-ownership.integration.test.ts` (mesmo boilerplate de usuários dos testes RLS existentes):

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { createClient, type SupabaseClient } from '@supabase/supabase-js'
import { userOwnsList } from './list-ownership'

const url = process.env.NEXT_PUBLIC_SUPABASE_URL!
const anonKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!

const admin = createClient(url, serviceKey, {
  auth: { autoRefreshToken: false, persistSession: false },
})

type TestUser = { id: string; email: string; client: SupabaseClient }

async function makeUser(): Promise<TestUser> {
  const email = `own-${crypto.randomUUID()}@test.local`
  const password = 'test-password-123'
  const { data, error } = await admin.auth.admin.createUser({
    email,
    password,
    email_confirm: true,
  })
  if (error) throw error

  const client = createClient(url, anonKey, {
    auth: { autoRefreshToken: false, persistSession: false },
  })
  const { error: signInError } = await client.auth.signInWithPassword({ email, password })
  if (signInError) throw signInError

  return { id: data.user!.id, email, client }
}

describe('userOwnsList sob RLS (GC-21)', () => {
  let userA: TestUser
  let userB: TestUser
  let listAId: string

  beforeAll(async () => {
    userA = await makeUser()
    userB = await makeUser()

    const { data, error } = await userA.client
      .from('lists')
      .insert({ user_id: userA.id, name: 'Lista de A' })
      .select('id')
      .single()
    if (error) throw error
    listAId = data.id
  })

  afterAll(async () => {
    if (userA) await admin.auth.admin.deleteUser(userA.id)
    if (userB) await admin.auth.admin.deleteUser(userB.id)
  })

  it('dono da lista → true (fluxo normal intacto)', async () => {
    expect(await userOwnsList(userA.client, listAId)).toBe(true)
  })

  it('lista de outro usuário → false (RLS esconde a linha)', async () => {
    expect(await userOwnsList(userB.client, listAId)).toBe(false)
  })

  it('id inexistente → false', async () => {
    expect(await userOwnsList(userA.client, crypto.randomUUID())).toBe(false)
  })

  it('id malformado → false (erro do banco tratado)', async () => {
    expect(await userOwnsList(userA.client, 'não-é-uuid')).toBe(false)
  })
})
```

Run: `npm run test:integration` — Expected: PASS (Supabase local no ar).

- [ ] **Step 6: Gates**

Run: `npx vitest --run && npm run lint && npm run typecheck` — Expected: tudo verde.

- [ ] **Step 7: Commit**

```bash
git add src/lib/supabase/list-ownership.ts src/lib/supabase/list-ownership.integration.test.ts src/app/actions.ts src/app/actions.test.ts
git commit -m "GC-21 createTask valida posse do list_id sob RLS do usuário"
```

- [ ] **Step 8: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-21-createtask-validar-posse-do-list_id-recebido-do-form
gh pr create --title "GC-21 createTask: validar posse do list_id recebido do form" --body "Helper userOwnsList (select id em lists com client RLS do usuário); createTask rejeita list_id de lista alheia/inexistente pelo canal de erro do form. Unit do wiring + integração com 2 usuários.

Fixes GC-21

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

---

### Task 4: GC-22 · Logout com escopo local

`signOut()` sem argumento revoga **todas** as sessões da conta (escopo global). Trocar por `signOut({ scope: 'local' })` em [login/actions.ts:21-25](../../../src/app/login/actions.ts). **Fora de escopo:** paralelizar o Playwright (`workers: 1` fica como está — anotado na issue como follow-up com risco próprio).

**Files:**
- Create: `src/app/login/actions.test.ts`
- Modify: `src/app/login/actions.ts`

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-22-logout-com-escopo-local-signout-scope-local
```

- [ ] **Step 2: Unit test da action (falha primeiro)**

Criar `src/app/login/actions.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'

const signOutMock = vi.fn(async () => ({ error: null }))
const redirectMock = vi.fn()

vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(async () => ({ auth: { signOut: signOutMock } })),
}))
vi.mock('next/navigation', () => ({
  redirect: (...args: unknown[]) => redirectMock(...args),
}))

import { signOut } from './actions'

describe('signOut (GC-22)', () => {
  it("encerra só a sessão corrente (scope 'local') e volta ao login", async () => {
    await signOut()
    expect(signOutMock).toHaveBeenCalledWith({ scope: 'local' })
    expect(redirectMock).toHaveBeenCalledWith('/login')
  })
})
```

Run: `npx vitest --run src/app/login/actions.test.ts`
Expected: FAIL — `signOut` foi chamado sem argumentos (`toHaveBeenCalledWith({ scope: 'local' })` não bate).

- [ ] **Step 3: Implementar**

Em `src/app/login/actions.ts`, trocar o corpo de `signOut`:

```ts
export async function signOut() {
  const supabase = await createClient()
  // GC-22: escopo local — encerra só a sessão corrente, não todas as da conta.
  await supabase.auth.signOut({ scope: 'local' })
  redirect('/login')
}
```

Run: `npx vitest --run src/app/login/actions.test.ts` — Expected: PASS.

- [ ] **Step 4: Gates (E2E de auth cobre o fluxo real)**

Run: `npx vitest --run && npm run lint && npm run typecheck` — Expected: verde.
Run: `npx playwright test e2e/auth.spec.ts` — Expected: PASS (login → logout → volta ao login).

- [ ] **Step 5: Commit**

```bash
git add src/app/login/actions.ts src/app/login/actions.test.ts
git commit -m "GC-22 logout com scope local (não revoga as outras sessões)"
```

- [ ] **Step 6: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-22-logout-com-escopo-local-signout-scope-local
gh pr create --title "GC-22 Logout com escopo local (signOut scope: 'local')" --body "signOut({ scope: 'local' }) encerra só a sessão corrente. Unit asserta o argumento; e2e/auth.spec cobre o fluxo. Paralelizar o Playwright (workers: 1) fica como follow-up anotado na issue.

Fixes GC-22

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

---

### Task 5: GC-23 · `deleteList` com cascade + limpeza do E2E

Decisão aprovada: apagar a lista **apaga as tarefas junto**. Migration troca a FK `tasks.list_id` de `on delete set null` para `on delete cascade`; server action `deleteList` (posse garantida pela RLS de `lists`); botão "Apagar lista" junto à aba ativa com `confirm()` nativo deixando explícito que as tarefas vão junto; E2E de listas passa a apagar a lista no fim (duas execuções seguidas não acumulam listas).

**Files:**
- Create: `supabase/migrations/20260723120000_tasks_list_id_cascade.sql`
- Create: `src/app/delete-list-button.tsx`, `src/app/delete-list-button.test.tsx`
- Create: `src/lib/supabase/lists-cascade.integration.test.ts`
- Modify: `src/app/actions.ts` (nova action `deleteList`)
- Modify: `src/app/list-tabs.tsx`
- Modify: `e2e/lists.spec.ts`

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-23-deletelist-limpeza-de-dados-residuais-do-e2e
```

- [ ] **Step 2: Migration do cascade**

Criar `supabase/migrations/20260723120000_tasks_list_id_cascade.sql`:

```sql
-- GC-23: apagar a lista apaga as tarefas junto (decisão do design v1.1).
-- Recria a FK de tasks.list_id trocando "on delete set null" por cascade.
alter table public.tasks drop constraint tasks_list_id_fkey;

alter table public.tasks
  add constraint tasks_list_id_fkey
  foreign key (list_id) references public.lists (id) on delete cascade;
```

Aplicar no Supabase local: `npx supabase migration up` — Expected: aplica sem erro.

- [ ] **Step 3: Integration test do cascade + RLS (valida a migration do Step 2 — sem ela, falha)**

Criar `src/lib/supabase/lists-cascade.integration.test.ts`:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { createClient, type SupabaseClient } from '@supabase/supabase-js'

const url = process.env.NEXT_PUBLIC_SUPABASE_URL!
const anonKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!

const admin = createClient(url, serviceKey, {
  auth: { autoRefreshToken: false, persistSession: false },
})

type TestUser = { id: string; email: string; client: SupabaseClient }

async function makeUser(): Promise<TestUser> {
  const email = `cascade-${crypto.randomUUID()}@test.local`
  const password = 'test-password-123'
  const { data, error } = await admin.auth.admin.createUser({
    email,
    password,
    email_confirm: true,
  })
  if (error) throw error

  const client = createClient(url, anonKey, {
    auth: { autoRefreshToken: false, persistSession: false },
  })
  const { error: signInError } = await client.auth.signInWithPassword({ email, password })
  if (signInError) throw signInError

  return { id: data.user!.id, email, client }
}

describe('deleteList: cascade e RLS (GC-23)', () => {
  let userA: TestUser
  let userB: TestUser
  let listAId: string
  let taskInListId: string
  let looseTaskId: string

  beforeAll(async () => {
    userA = await makeUser()
    userB = await makeUser()

    const { data: list, error: listErr } = await userA.client
      .from('lists')
      .insert({ user_id: userA.id, name: 'Lista com tasks' })
      .select('id')
      .single()
    if (listErr) throw listErr
    listAId = list.id

    const { data: t1, error: t1Err } = await userA.client
      .from('tasks')
      .insert({ user_id: userA.id, title: 'Na lista', list_id: listAId })
      .select('id')
      .single()
    if (t1Err) throw t1Err
    taskInListId = t1.id

    const { data: t2, error: t2Err } = await userA.client
      .from('tasks')
      .insert({ user_id: userA.id, title: 'Sem lista' })
      .select('id')
      .single()
    if (t2Err) throw t2Err
    looseTaskId = t2.id
  })

  afterAll(async () => {
    if (userA) await admin.auth.admin.deleteUser(userA.id)
    if (userB) await admin.auth.admin.deleteUser(userB.id)
  })

  it('B não consegue apagar a lista de A (RLS filtra; nada muda)', async () => {
    await userB.client.from('lists').delete().eq('id', listAId)

    const { data: list } = await userA.client.from('lists').select('id').eq('id', listAId)
    expect(list ?? []).toHaveLength(1)
    const { data: task } = await userA.client.from('tasks').select('id').eq('id', taskInListId)
    expect(task ?? []).toHaveLength(1)
  })

  it('A apaga a lista → as tasks da lista somem junto (cascade)', async () => {
    const { error } = await userA.client.from('lists').delete().eq('id', listAId)
    expect(error).toBeNull()

    const { data: gone } = await userA.client.from('tasks').select('id').eq('id', taskInListId)
    expect(gone ?? []).toHaveLength(0)
  })

  it('task sem lista não é afetada pelo cascade', async () => {
    const { data: still } = await userA.client.from('tasks').select('id').eq('id', looseTaskId)
    expect(still ?? []).toHaveLength(1)
  })
})
```

Run: `npm run test:integration` — Expected: PASS (a migration do Step 2 já está aplicada; os `it` rodam em ordem no arquivo).

- [ ] **Step 4: Unit test do botão com confirm (falha primeiro)**

Criar `src/app/delete-list-button.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'

vi.mock('./actions', () => ({ deleteList: vi.fn() }))

import { DeleteListButton } from './delete-list-button'
import { deleteList } from './actions'

describe('DeleteListButton (GC-23)', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
    vi.mocked(deleteList).mockReset()
  })

  it('confirm cancelado → não submete a action', () => {
    const confirmSpy = vi.spyOn(window, 'confirm').mockReturnValue(false)
    render(<DeleteListButton listId="l1" listName="Mercado" />)
    fireEvent.click(screen.getByRole('button', { name: 'Apagar lista Mercado' }))
    expect(confirmSpy).toHaveBeenCalledOnce()
    expect(deleteList).not.toHaveBeenCalled()
  })

  it('o texto do confirm deixa inequívoco que as tarefas serão apagadas', () => {
    const confirmSpy = vi.spyOn(window, 'confirm').mockReturnValue(false)
    render(<DeleteListButton listId="l1" listName="Mercado" />)
    fireEvent.click(screen.getByRole('button', { name: 'Apagar lista Mercado' }))
    expect(confirmSpy).toHaveBeenCalledWith(expect.stringMatching(/tarefas.*apagadas/i))
  })

  it('confirm aceito → submete a action', async () => {
    vi.spyOn(window, 'confirm').mockReturnValue(true)
    render(<DeleteListButton listId="l1" listName="Mercado" />)
    fireEvent.click(screen.getByRole('button', { name: 'Apagar lista Mercado' }))
    await waitFor(() => expect(deleteList).toHaveBeenCalledOnce())
  })
})
```

Run: `npx vitest --run src/app/delete-list-button.test.tsx` — Expected: FAIL (componente e action não existem).

- [ ] **Step 5: Action `deleteList` + componente + aba**

Em `src/app/actions.ts`, acrescentar ao final:

```ts
/** GC-23: apagar lista — as tarefas vão junto (FK on delete cascade). RLS garante a posse. */
export async function deleteList(formData: FormData) {
  const id = String(formData.get('id') ?? '')
  const { supabase } = await requireUserClient()
  await supabase.from('lists').delete().eq('id', id)
  revalidatePath('/')
  // O botão vive na aba ativa: depois de apagar, a UI volta para "Todas".
  redirect('/')
}
```

Criar `src/app/delete-list-button.tsx`:

```tsx
'use client'

import { deleteList } from './actions'

/** GC-23: apaga a lista ativa; o confirm() nativo é a única barreira — texto inequívoco. */
export function DeleteListButton({ listId, listName }: { listId: string; listName: string }) {
  return (
    <form
      action={deleteList}
      onSubmit={(e) => {
        const ok = confirm(
          `Apagar a lista "${listName}"? As tarefas desta lista serão apagadas junto.`,
        )
        if (!ok) e.preventDefault()
      }}
    >
      <input type="hidden" name="id" value={listId} />
      <button type="submit" aria-label={`Apagar lista ${listName}`}>
        Apagar lista
      </button>
    </form>
  )
}
```

Substituir o conteúdo de `src/app/list-tabs.tsx` por:

```tsx
import Link from 'next/link'
import type { List } from '@/domain/list'
import { NewList } from './new-list'
import { DeleteListButton } from './delete-list-button'

/** RF-2.2: pílulas "Todas" + uma por lista + "＋ Nova lista". Aba ativa via aria-current. */
export function ListTabs({
  lists,
  activeListId,
}: {
  lists: List[]
  activeListId: string | null
}) {
  return (
    <nav
      aria-label="Listas"
      style={{ display: 'flex', gap: 8, flexWrap: 'wrap', alignItems: 'center', margin: '16px 0' }}
    >
      <Link href="/" aria-current={activeListId === null ? 'page' : undefined}>
        Todas
      </Link>
      {lists.map((list) => (
        <span key={list.id} style={{ display: 'inline-flex', gap: 4, alignItems: 'center' }}>
          <Link
            href={`/?list=${list.id}`}
            aria-current={activeListId === list.id ? 'page' : undefined}
          >
            {list.name}
          </Link>
          {/* GC-23: apagar só a lista ativa — o usuário está vendo o que vai apagar. */}
          {activeListId === list.id ? (
            <DeleteListButton listId={list.id} listName={list.name} />
          ) : null}
        </span>
      ))}
      <NewList />
    </nav>
  )
}
```

Run: `npx vitest --run src/app/delete-list-button.test.tsx` — Expected: PASS.

- [ ] **Step 6: Gates unit + commit parcial**

Run: `npx vitest --run && npm run lint && npm run typecheck` — Expected: verde.

```bash
git add supabase/migrations/20260723120000_tasks_list_id_cascade.sql src/app/actions.ts src/app/delete-list-button.tsx src/app/delete-list-button.test.tsx src/app/list-tabs.tsx src/lib/supabase/lists-cascade.integration.test.ts
git commit -m "GC-23 deleteList com cascade, botão na aba ativa e confirm explícito"
```

- [ ] **Step 7: E2E — apagar a lista no fim do fluxo (não-acúmulo)**

Substituir o conteúdo de `e2e/lists.spec.ts` por:

```ts
import { test, expect } from '@playwright/test'

const EMAIL = process.env.E2E_OWNER_EMAIL ?? ''
const PASSWORD = process.env.E2E_OWNER_PASSWORD ?? ''

test.describe('Listas (GC-15 / GC-23)', () => {
  test('criar lista → tarefa na lista → apagar a lista leva as tarefas junto (sem acúmulo)', async ({
    page,
  }) => {
    test.skip(!EMAIL || !PASSWORD, 'defina E2E_OWNER_EMAIL/E2E_OWNER_PASSWORD no .env.local')

    // Login
    await page.goto('/login')
    await page.fill('input[name="email"]', EMAIL)
    await page.fill('input[name="password"]', PASSWORD)
    await page.getByRole('button', { name: /Entrar/ }).click()
    await expect(page).toHaveURL(/localhost:\d+\/$/)

    const stamp = Date.now()
    const listName = `Lista E2E ${stamp}`
    const taskInList = `Tarefa na lista ${stamp}`

    // Criar lista
    await page.getByLabel('Nome da nova lista').fill(listName)
    await page.getByRole('button', { name: 'Criar' }).click()
    const listTab = page.getByRole('link', { name: listName })
    await expect(listTab).toBeVisible()

    // Ir para a aba da lista e criar uma tarefa nela
    await listTab.click()
    await expect(page).toHaveURL(/\?list=/)
    await page.getByLabel('Título da tarefa').fill(taskInList)
    await page.getByRole('button', { name: 'Adicionar' }).click()
    await expect(page.getByText(taskInList)).toBeVisible()
    // Espera a action de criação assentar (botão sai de "Adicionando…"): clicar num Link
    // durante o commit da action deixava o router mudo e o POST seguinte nunca disparava.
    await expect(page.getByRole('button', { name: 'Adicionar' })).toBeEnabled()

    // Aba "Todas" também mostra a tarefa (tem lista, mas "Todas" mostra tudo)
    await page.getByRole('link', { name: 'Todas' }).click()
    await expect(page).toHaveURL(/localhost:\d+\/$/)
    await expect(page.getByText(taskInList)).toBeVisible()

    // Voltar à lista e apagá-la: cascade apaga a tarefa e a UI volta para "Todas" (GC-23).
    await listTab.click()
    await expect(page).toHaveURL(/\?list=/)
    page.once('dialog', (dialog) => dialog.accept())
    await page.getByRole('button', { name: `Apagar lista ${listName}` }).click()
    await expect(page).toHaveURL(/localhost:\d+\/$/)

    // Não-acúmulo: nem a lista nem a tarefa sobram para o próximo run.
    await expect(page.getByRole('link', { name: listName })).toHaveCount(0)
    await expect(page.getByText(taskInList)).toHaveCount(0)
  })
})
```

Run: `npm run e2e` — Expected: PASS (suíte completa; rodar duas vezes e conferir que a segunda também passa — sem lista acumulada).

- [ ] **Step 8: Commit**

```bash
git add e2e/lists.spec.ts
git commit -m "GC-23 e2e: fluxo apagar lista com cascade e verificação de não-acúmulo"
```

- [ ] **Step 9: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-23-deletelist-limpeza-de-dados-residuais-do-e2e
gh pr create --title "GC-23 deleteList + limpeza de dados residuais do E2E" --body "Migration troca a FK tasks.list_id para on delete cascade; server action deleteList (RLS garante posse, redirect para Todas); botão 'Apagar lista' na aba ativa com confirm() explícito sobre as tarefas; E2E de listas apaga a lista no teardown (sem acúmulo entre runs).

Atenção pós-merge: rodar npx supabase migration up antes de integration/E2E locais.

Fixes GC-23

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

---

### Task 6: GC-24 · Índice em `tasks.list_id` + filtro no banco

O filtro por lista roda **em memória** (`filterTasksByList` em [domain/list.ts:44-50](../../../src/domain/list.ts), usada em [page.tsx:63](../../../src/app/page.tsx)) e não há índice em `tasks.list_id`. Mover o filtro para a query (`.eq('list_id', ...)`) quando há `?list=` válido, criar o índice e **remover** `filterTasksByList` (as demais funções puras ficam). Comportamento das abas idêntico.

**Files:**
- Create: `supabase/migrations/20260723130000_tasks_list_id_index.sql`
- Create: `src/lib/supabase/tasks-list-filter.integration.test.ts`
- Modify: `src/app/page.tsx`
- Modify: `src/domain/list.ts`, `src/domain/list.test.ts` (remoção)

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-24-indice-em-taskslist_id-e-filtro-por-lista-no-banco
```

- [ ] **Step 2: Migration do índice**

Criar `supabase/migrations/20260723130000_tasks_list_id_index.sql`:

```sql
-- GC-24: o filtro por lista agora roda no banco (.eq('list_id', ...)); índice para a busca.
create index tasks_list_id_idx on public.tasks (list_id);
```

Aplicar: `npx supabase migration up` — Expected: aplica sem erro.

- [ ] **Step 3: Integration test do filtro no banco**

Criar `src/lib/supabase/tasks-list-filter.integration.test.ts`:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { createClient, type SupabaseClient } from '@supabase/supabase-js'

const url = process.env.NEXT_PUBLIC_SUPABASE_URL!
const anonKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!

const admin = createClient(url, serviceKey, {
  auth: { autoRefreshToken: false, persistSession: false },
})

type TestUser = { id: string; email: string; client: SupabaseClient }

async function makeUser(): Promise<TestUser> {
  const email = `filter-${crypto.randomUUID()}@test.local`
  const password = 'test-password-123'
  const { data, error } = await admin.auth.admin.createUser({
    email,
    password,
    email_confirm: true,
  })
  if (error) throw error

  const client = createClient(url, anonKey, {
    auth: { autoRefreshToken: false, persistSession: false },
  })
  const { error: signInError } = await client.auth.signInWithPassword({ email, password })
  if (signInError) throw signInError

  return { id: data.user!.id, email, client }
}

describe('filtro de tasks por list_id no banco (GC-24)', () => {
  let user: TestUser
  let list1: string
  let list2: string

  beforeAll(async () => {
    user = await makeUser()

    const { data: lists, error: listErr } = await user.client
      .from('lists')
      .insert([
        { user_id: user.id, name: 'L1' },
        { user_id: user.id, name: 'L2' },
      ])
      .select('id,name')
    if (listErr) throw listErr
    list1 = lists!.find((l) => l.name === 'L1')!.id
    list2 = lists!.find((l) => l.name === 'L2')!.id

    const { error: taskErr } = await user.client.from('tasks').insert([
      { user_id: user.id, title: 'na L1', list_id: list1 },
      { user_id: user.id, title: 'na L2', list_id: list2 },
      { user_id: user.id, title: 'sem lista' },
    ])
    if (taskErr) throw taskErr
  })

  afterAll(async () => {
    if (user) await admin.auth.admin.deleteUser(user.id)
  })

  it('eq(list_id, L1) devolve só a task de L1 (task de outra lista não vem)', async () => {
    const { data, error } = await user.client
      .from('tasks')
      .select('title')
      .eq('list_id', list1)
    expect(error).toBeNull()
    expect((data ?? []).map((t) => t.title)).toEqual(['na L1'])
  })

  it('sem filtro (aba "Todas") devolve as três', async () => {
    const { data, error } = await user.client.from('tasks').select('title')
    expect(error).toBeNull()
    expect(data ?? []).toHaveLength(3)
  })
})
```

Run: `npm run test:integration` — Expected: PASS.

- [ ] **Step 4: Filtrar na query em `src/app/page.tsx`**

Trocar o bloco de busca de tasks (linhas 59–63):

```ts
  const { data } = await supabase
    .from('tasks')
    .select('id,title,priority,due_date,status,list_id,created_at')
  const allTasks = (data ?? []) as Task[]
  const tasks = filterTasksByList(allTasks, activeListId)
```

por:

```ts
  // GC-24: filtro por lista no banco (índice tasks_list_id_idx), não em memória.
  let tasksQuery = supabase
    .from('tasks')
    .select('id,title,priority,due_date,status,list_id,created_at')
  if (activeListId !== null) tasksQuery = tasksQuery.eq('list_id', activeListId)
  const { data } = await tasksQuery
  const tasks = (data ?? []) as Task[]
```

E no import de `@/domain/list` (linhas 15–20), remover `filterTasksByList,` (ficam `resolveActiveListId`, `ALL_LISTS`, `type List`).

- [ ] **Step 5: Remover `filterTasksByList` do domínio**

Em `src/domain/list.ts`: apagar a função `filterTasksByList` inteira (com o comentário RF-2.3, linhas 43–50).

Em `src/domain/list.test.ts`: apagar o `describe('filterTasksByList (RF-2.3)', ...)` inteiro e remover `filterTasksByList,` do import.

- [ ] **Step 6: Gates**

Run: `npx vitest --run && npm run lint && npm run typecheck` — Expected: verde (typecheck garante que não sobrou referência).
Run: `npx playwright test e2e/lists.spec.ts` — Expected: PASS (comportamento das abas idêntico).

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/20260723130000_tasks_list_id_index.sql src/lib/supabase/tasks-list-filter.integration.test.ts src/app/page.tsx src/domain/list.ts src/domain/list.test.ts
git commit -m "GC-24 filtro por lista no banco com indice em tasks.list_id"
```

- [ ] **Step 8: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-24-indice-em-taskslist_id-e-filtro-por-lista-no-banco
gh pr create --title "GC-24 Índice em tasks.list_id e filtro por lista no banco" --body "Migration cria tasks_list_id_idx; a página filtra com .eq('list_id', ...) quando há ?list= válido; filterTasksByList removida do domínio (demais funções puras ficam). Integração cobre o filtro; e2e de listas segue verde.

Atenção pós-merge: rodar npx supabase migration up antes de integration/E2E locais.

Fixes GC-24

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

---

### Task 7: GC-25 · Robustez da suíte de testes

Dois follow-ups só de teste: (1) `e2e/theme.spec.ts` assume dark no início — fixar o estado com `context.addCookies` no setup; (2) padronizar `{ exact: true }` em **todos** os `getByLabel` dos specs E2E — os rótulos da página seguem o padrão "X de T" / "Aplicar X de T" (ex.: `Prioridade de T` é substring case-insensitive de `Aplicar prioridade de T`), e o match default por substring gera strict mode violation assim que alguém usa esses rótulos (os `Status de` já levavam `exact` por isso; nenhum spec usa prioridade/prazo hoje — a padronização fecha a porta).

**Files:**
- Modify: `e2e/theme.spec.ts`
- Modify: `e2e/lists.spec.ts`, `e2e/tasks.spec.ts`, `e2e/view.spec.ts`

- [ ] **Step 1: Branch**

```bash
git checkout main && git pull
git checkout -b glaubercosta/gc-25-robustez-da-suite-de-testes-reset-de-cookie-no-themespec-e
```

- [ ] **Step 2: Fixar o estado inicial do tema no spec**

Em `e2e/theme.spec.ts`, logo após `test.describe('Tema (GC-17)', () => {`, adicionar:

```ts
  // GC-25: fixa o estado inicial — um run abortado no meio (que deixou tema claro
  // aplicado) não pode quebrar o run seguinte; nada de assumir dark.
  test.beforeEach(async ({ context, baseURL }) => {
    await context.addCookies([{ name: 'theme', value: 'dark', url: baseURL! }])
  })
```

E trocar a asserção que "assume" o padrão (linha 16):

```ts
    await expect(page.locator('html')).not.toHaveAttribute('data-theme', 'light')
```

por (agora o estado é determinístico, o SSR entrega `data-theme="dark"`):

```ts
    await expect(page.locator('html')).toHaveAttribute('data-theme', 'dark')
```

Atualizar também o comentário do login (linha 10) para: `// Login (cookie de tema fixado em dark no beforeEach — estado determinístico)`.

- [ ] **Step 3: `{ exact: true }` em todos os `getByLabel` dos specs**

Em `e2e/lists.spec.ts`:
- `page.getByLabel('Nome da nova lista')` → `page.getByLabel('Nome da nova lista', { exact: true })`
- `page.getByLabel('Título da tarefa')` → `page.getByLabel('Título da tarefa', { exact: true })`

Em `e2e/tasks.spec.ts`:
- `page.getByLabel('Título da tarefa')` → `page.getByLabel('Título da tarefa', { exact: true })`
- ``page.getByLabel(`Editar título de ${unique}`)`` → ``page.getByLabel(`Editar título de ${unique}`, { exact: true })``
- (`Status de ${edited}` já tem `{ exact: true }` — conferir e manter)

Em `e2e/view.spec.ts`:
- `page.getByLabel('Título da tarefa')` → `page.getByLabel('Título da tarefa', { exact: true })`
- (`Status de ${unique}` já tem `{ exact: true }` — conferir e manter)

- [ ] **Step 4: Verificar — suíte verde, inclusive após run interrompido**

Run: `npm run e2e` — Expected: PASS.
Simular o run abortado: rodar de novo imediatamente — Expected: PASS (o `beforeEach` fixa o cookie; nenhum estado herdado importa).
Run: `npx playwright test e2e/theme.spec.ts` duas vezes seguidas — Expected: PASS nas duas.

- [ ] **Step 5: Commit**

```bash
git add e2e/theme.spec.ts e2e/lists.spec.ts e2e/tasks.spec.ts e2e/view.spec.ts
git commit -m "GC-25 e2e: cookie de tema fixado no setup e getByLabel com exact"
```

- [ ] **Step 6: PR, ci verde, merge**

```bash
git push -u origin glaubercosta/gc-25-robustez-da-suite-de-testes-reset-de-cookie-no-themespec-e
gh pr create --title "GC-25 Robustez da suíte de testes: reset de cookie no theme.spec e getByLabel exato" --body "theme.spec fixa o estado inicial com context.addCookies (theme=dark) em vez de assumir; todos os getByLabel dos specs E2E padronizados com { exact: true } (rótulos 'X de T' / 'Aplicar X de T' colidem em substring).

Fixes GC-25

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
gh pr checks --watch
gh pr merge --merge --delete-branch
git checkout main && git pull
```

---

## Encerramento do épico

- [ ] Conferir no Linear que GC-19..GC-25 estão **Done** (automação via merge) e que o épico GC-18 pode ser fechado.
- [ ] Rodar a suíte completa na `main` final: `npx vitest --run && npm run test:integration && npm run e2e` — tudo verde.
