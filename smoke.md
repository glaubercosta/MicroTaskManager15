# Smoke test — GC-5

Arquivo trivial para exercitar o fluxo Linear ↔ GitHub ponta a ponta:
issue → branch → PR (`Fixes GC-5`) → check `ci` → merge → status automático.

Pode ser removido após a validação.
