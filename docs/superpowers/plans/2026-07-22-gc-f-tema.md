# GC-f · Tema + identidade visual — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Linear:** GC-17 (GC-f). Depende de GC-c..GC-e (todas done). Última story do épico GC-13.

**Goal:** Fechar a RF-6 do PRD: tokens CSS (dark padrão, light espelhado), tema persistido em cookie e resolvido no servidor (sem flash) com fallback localStorage, toggle no menu da conta, layout coluna única (~600px) sob barra de topo, Geist Sans, accent violeta parcimonioso, pontinho de prioridade monocromático, `prefers-reduced-motion` respeitado.

**Architecture:** A lógica de tema é pura em `src/domain/theme.ts` (`parseTheme`, `toggleTheme`, constantes) — sem DOM, testável por igualdade. O servidor (`layout.tsx`) lê o cookie `theme` e estampa `data-theme` no `<html>` no SSR → zero flash. Quando **não há cookie**, um script inline bloqueante no topo do `<body>` aplica o fallback localStorage antes do paint e regrava o cookie. O toggle é um client component que muda `data-theme`, cookie e localStorage sem round-trip; o próximo SSR lê o cookie. Todas as cores viram tokens em `globals.css` (dark no `:root`, light em `:root[data-theme="light"]`); componentes trocam hex inline por `var(...)`.

**Tech Stack:** Next.js 16 (App Router, Server Components + 1 client component), TypeScript, Vitest 4 + @testing-library/react (jsdom), Playwright (E2E fora do gate `ci`).

---

## File Structure

- **Create** `src/domain/theme.ts` — `Theme`, `THEMES`, `DEFAULT_THEME`, `THEME_COOKIE`, `THEME_COOKIE_MAX_AGE`, `parseTheme`, `toggleTheme`.
- **Create** `src/domain/theme.test.ts` — testes das funções puras.
- **Modify** `src/app/globals.css` — tokens dark/light, base (Geist, color-scheme), `.topbar`, `.container`, `.account-menu`, `.priority-dot`, `prefers-reduced-motion`.
- **Modify** `src/app/layout.tsx` — `lang="pt-BR"`, metadata real, cookie → `data-theme`, script inline de fallback, `suppressHydrationWarning`.
- **Create** `src/app/theme-toggle.tsx` + `src/app/theme-toggle.test.tsx` — client component do toggle.
- **Create** `src/app/account-menu.tsx` + `src/app/account-menu.test.tsx` — `<details>` com e-mail, toggle e Sair (action via prop, para ser testável).
- **Modify** `src/app/page.tsx` — barra de topo + `AccountMenu`, `main.container`, remove header/`Sessão de` antigos.
- **Modify** `src/app/priority-dot.tsx` + `src/app/priority-dot.test.tsx` — monocromático com peso por opacidade (`data-priority`).
- **Modify** `src/app/due-badge.tsx` — cores → tokens.
- **Modify** `src/app/login/page.tsx` — classes/tokens no lugar de inline style.
- **Modify** `e2e/auth.spec.ts` — "Sair" agora está dentro do menu da conta.
- **Create** `e2e/theme.spec.ts` — toggle persiste entre reloads, SSR sem flash.

**Convenções (design §5):** valores internos em inglês (`dark`/`light`), rótulos pt-BR; TDD; commits frequentes com `GC-17` na mensagem; **não** referenciar issues concluídas no PR.

---

## Task 0: Branch + housekeeping

- [ ] **Step 1: Criar a branch a partir da `main` atualizada**

```bash
git checkout main && git pull origin main
git checkout -b glaubercosta/gc-17-gc-f-tema-identidade-visual
```

- [ ] **Step 2: Commitar os planos pendentes (GC-e ficou fora do PR #12; este plano)**

```bash
git add docs/superpowers/plans/2026-07-21-gc-e-visualizacao.md docs/superpowers/plans/2026-07-22-gc-f-tema.md
git commit -m "GC-17 docs: plano da GC-f + plano da GC-e (housekeeping)"
```

---

## Task 1: Domínio puro de tema (RF-6)

**Files:**
- Create: `src/domain/theme.ts`
- Test: `src/domain/theme.test.ts`

- [ ] **Step 1: Write the failing test**

Criar `src/domain/theme.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import {
  THEMES,
  DEFAULT_THEME,
  THEME_COOKIE,
  parseTheme,
  toggleTheme,
} from './theme'

describe('tema (RF-6)', () => {
  it('dark é o tema padrão', () => {
    expect(DEFAULT_THEME).toBe('dark')
    expect(THEMES).toEqual(['dark', 'light'])
  })

  it('parseTheme aceita apenas valores válidos', () => {
    expect(parseTheme('dark')).toBe('dark')
    expect(parseTheme('light')).toBe('light')
  })

  it('parseTheme cai no padrão para lixo/ausência (cookie adulterado, RNF)', () => {
    expect(parseTheme(undefined)).toBe('dark')
    expect(parseTheme(null)).toBe('dark')
    expect(parseTheme('')).toBe('dark')
    expect(parseTheme('LIGHT')).toBe('dark')
    expect(parseTheme('solarized')).toBe('dark')
  })

  it('toggleTheme alterna entre os dois temas', () => {
    expect(toggleTheme('dark')).toBe('light')
    expect(toggleTheme('light')).toBe('dark')
  })

  it('nome do cookie é estável (contrato com layout/toggle/script inline)', () => {
    expect(THEME_COOKIE).toBe('theme')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- --run src/domain/theme.test.ts`
Expected: FAIL — `Failed to resolve import "./theme"`.

- [ ] **Step 3: Write minimal implementation**

Criar `src/domain/theme.ts`:

```ts
export type Theme = 'dark' | 'light'

export const THEMES: readonly Theme[] = ['dark', 'light']

/** Escuro por padrão (RF-6). */
export const DEFAULT_THEME: Theme = 'dark'

/** Nome do cookie/chave localStorage — contrato com layout, toggle e script inline. */
export const THEME_COOKIE = 'theme'

/** 1 ano. */
export const THEME_COOKIE_MAX_AGE = 60 * 60 * 24 * 365

/** Valida valor vindo de cookie/localStorage; qualquer coisa inválida → padrão. */
export function parseTheme(value: string | null | undefined): Theme {
  return (THEMES as readonly string[]).includes(value ?? '')
    ? (value as Theme)
    : DEFAULT_THEME
}

export function toggleTheme(theme: Theme): Theme {
  return theme === 'dark' ? 'light' : 'dark'
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- --run src/domain/theme.test.ts`
Expected: PASS (5 testes).

- [ ] **Step 5: Commit**

```bash
git add src/domain/theme.ts src/domain/theme.test.ts
git commit -m "GC-17 dominio de tema: parseTheme/toggleTheme, dark padrao"
```

---

## Task 2: Tokens CSS + base visual em `globals.css`

CSS não tem teste unitário; o gate é typecheck/lint/testes existentes continuarem verdes + verificação visual nas tasks seguintes e no E2E.

**Files:**
- Modify: `src/app/globals.css` (substituir o arquivo inteiro)

- [ ] **Step 1: Substituir `globals.css` pelo conteúdo abaixo**

Dark no `:root` (padrão), light espelhado em `:root[data-theme="light"]`. Contraste AA verificado nos pares fg/bg e muted/bg. Accent violeta usado com parcimônia (links/foco). Remove os `@media (prefers-color-scheme)` — o tema agora é explícito via cookie.

```css
:root {
  color-scheme: dark;
  --background: #111113;
  --surface: #1b1b1f;
  --border: #2e2e35;
  --foreground: #ededf0;
  --muted: #a2a2ad;
  --accent: #a78bfa;
  --due-overdue: #f28b82;
  --due-today: #e2b04a;
}

:root[data-theme='light'] {
  color-scheme: light;
  --background: #ffffff;
  --surface: #f6f6f8;
  --border: #dcdce2;
  --foreground: #1c1c21;
  --muted: #5c5c66;
  --accent: #6d28d9;
  --due-overdue: #b3261e;
  --due-today: #8a5a00;
}

html {
  height: 100%;
}

html,
body {
  max-width: 100vw;
  overflow-x: hidden;
}

body {
  min-height: 100%;
  display: flex;
  flex-direction: column;
  color: var(--foreground);
  background: var(--background);
  font-family: var(--font-geist-sans), system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

a {
  color: inherit;
  text-decoration: none;
}

a:hover,
a:focus-visible {
  color: var(--accent);
}

:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

/* Barra de topo full-width; coluna única centralizada abaixo (RF-6/design §4). */
.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16px;
  padding: 12px 24px;
  border-bottom: 1px solid var(--border);
  background: var(--surface);
}

.brand {
  font-weight: 600;
}

.container {
  width: 100%;
  max-width: 600px;
  margin: 0 auto;
  padding: 24px;
}

/* Menu da conta: <details> nativo, sem JS. */
.account-menu {
  position: relative;
}

.account-menu > summary {
  cursor: pointer;
  color: var(--muted);
  list-style: none;
}

.account-menu > summary::-webkit-details-marker {
  display: none;
}

.account-menu > div {
  position: absolute;
  right: 0;
  margin-top: 8px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 12px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 8px;
  min-width: 180px;
  z-index: 10;
}

/* Pontinho de prioridade monocromático: peso por opacidade, não por cor (RF-4.2). */
.priority-dot {
  color: var(--foreground);
}

.priority-dot[data-priority='medium'] {
  opacity: 0.55;
}

.priority-dot[data-priority='low'] {
  opacity: 0.3;
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

- [ ] **Step 2: Verificar que nada quebrou**

Run: `npm run typecheck && npm run lint && npm test -- --run`
Expected: tudo verde (CSS não afeta os testes; gate de regressão).

- [ ] **Step 3: Commit**

```bash
git add src/app/globals.css
git commit -m "GC-17 tokens CSS: dark padrao, light espelhado, reduced-motion"
```

---

## Task 3: Layout SSR — cookie → `data-theme`, sem flash

**Files:**
- Modify: `src/app/layout.tsx` (substituir o arquivo inteiro)

A lógica condicional já é coberta por `parseTheme` (Task 1); o comportamento fim-a-fim (persistência sem flash) é coberto pelo E2E (Task 8). Sem teste unitário de layout (Server Component async com `next/headers` não roda em jsdom).

- [ ] **Step 1: Substituir `src/app/layout.tsx`**

Pontos: `lang="pt-BR"` (RNF-4, estava `en`); metadata real; cookie ausente → **não** estampa `data-theme` (deixa o script de fallback decidir; CSS já é dark por padrão); `suppressHydrationWarning` porque o script pode mutar `data-theme` antes da hidratação; script inline **bloqueante no topo do body** = aplica localStorage antes do primeiro paint e regrava o cookie para os próximos SSR.

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { cookies } from "next/headers";
import { parseTheme, THEME_COOKIE, THEME_COOKIE_MAX_AGE } from "@/domain/theme";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MicroTaskManager",
  description: "Gerenciador minimalista de tarefas pessoais",
};

/**
 * Fallback localStorage (RF-6): roda antes do paint quando NÃO há cookie.
 * Aplica o tema salvo e regrava o cookie para o próximo SSR resolver no servidor.
 */
const themeFallbackScript = `(function(){try{if(document.cookie.indexOf('${THEME_COOKIE}=')===-1){var t=localStorage.getItem('${THEME_COOKIE}');if(t==='light'||t==='dark'){document.documentElement.dataset.theme=t;document.cookie='${THEME_COOKIE}='+t+'; path=/; max-age=${THEME_COOKIE_MAX_AGE}; samesite=lax'}}}catch(e){}})()`;

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const raw = (await cookies()).get(THEME_COOKIE)?.value;
  const theme = raw === undefined ? undefined : parseTheme(raw);

  return (
    <html
      lang="pt-BR"
      data-theme={theme}
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body>
        <script dangerouslySetInnerHTML={{ __html: themeFallbackScript }} />
        {children}
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Verificar**

Run: `npm run typecheck && npm run lint && npm test -- --run`
Expected: verde.

- [ ] **Step 3: Commit**

```bash
git add src/app/layout.tsx
git commit -m "GC-17 layout: tema por cookie no SSR + fallback localStorage sem flash, lang pt-BR"
```

---

## Task 4: `ThemeToggle` (client component)

**Files:**
- Create: `src/app/theme-toggle.tsx`
- Test: `src/app/theme-toggle.test.tsx`

- [ ] **Step 1: Write the failing test**

Criar `src/app/theme-toggle.test.tsx`:

```tsx
import { describe, it, expect, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { ThemeToggle } from './theme-toggle'

describe('ThemeToggle (RF-6)', () => {
  beforeEach(() => {
    delete document.documentElement.dataset.theme
    document.cookie = 'theme=; path=/; max-age=0'
    localStorage.clear()
  })

  it('no dark, oferece mudar para claro', () => {
    render(<ThemeToggle initialTheme="dark" />)
    expect(screen.getByRole('button', { name: 'Usar tema claro' })).toBeInTheDocument()
  })

  it('clique aplica data-theme, cookie e localStorage e inverte o rótulo', () => {
    render(<ThemeToggle initialTheme="dark" />)
    fireEvent.click(screen.getByRole('button', { name: 'Usar tema claro' }))

    expect(document.documentElement.dataset.theme).toBe('light')
    expect(document.cookie).toContain('theme=light')
    expect(localStorage.getItem('theme')).toBe('light')
    expect(screen.getByRole('button', { name: 'Usar tema escuro' })).toBeInTheDocument()
  })

  it('segundo clique volta para dark', () => {
    render(<ThemeToggle initialTheme="dark" />)
    fireEvent.click(screen.getByRole('button', { name: 'Usar tema claro' }))
    fireEvent.click(screen.getByRole('button', { name: 'Usar tema escuro' }))

    expect(document.documentElement.dataset.theme).toBe('dark')
    expect(document.cookie).toContain('theme=dark')
    expect(localStorage.getItem('theme')).toBe('dark')
  })

  it('sincroniza com o data-theme aplicado pelo script de fallback (cookie ausente)', () => {
    document.documentElement.dataset.theme = 'light'
    render(<ThemeToggle initialTheme="dark" />)
    expect(screen.getByRole('button', { name: 'Usar tema escuro' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- --run src/app/theme-toggle.test.tsx`
Expected: FAIL — `Failed to resolve import "./theme-toggle"`.

- [ ] **Step 3: Write minimal implementation**

Criar `src/app/theme-toggle.tsx`:

```tsx
'use client'

import { useEffect, useState } from 'react'
import {
  parseTheme,
  toggleTheme,
  THEME_COOKIE,
  THEME_COOKIE_MAX_AGE,
  type Theme,
} from '@/domain/theme'

/**
 * Alterna o tema sem round-trip: muda o data-theme (paint imediato) e persiste
 * em cookie (próximo SSR resolve no servidor) + localStorage (fallback, RF-6).
 */
export function ThemeToggle({ initialTheme }: { initialTheme: Theme }) {
  const [theme, setTheme] = useState<Theme>(initialTheme)

  // O script inline de fallback pode ter aplicado localStorage antes da hidratação.
  useEffect(() => {
    const applied = document.documentElement.dataset.theme
    if (applied !== undefined) setTheme(parseTheme(applied))
  }, [])

  function handleClick() {
    const next = toggleTheme(theme)
    document.documentElement.dataset.theme = next
    document.cookie = `${THEME_COOKIE}=${next}; path=/; max-age=${THEME_COOKIE_MAX_AGE}; samesite=lax`
    try {
      localStorage.setItem(THEME_COOKIE, next)
    } catch {
      // localStorage indisponível (ex.: bloqueado) — cookie já persiste.
    }
    setTheme(next)
  }

  return (
    <button type="button" onClick={handleClick}>
      {theme === 'dark' ? 'Usar tema claro' : 'Usar tema escuro'}
    </button>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- --run src/app/theme-toggle.test.tsx`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
git add src/app/theme-toggle.tsx src/app/theme-toggle.test.tsx
git commit -m "GC-17 ThemeToggle: alterna data-theme e persiste em cookie + localStorage"
```

---

## Task 5: `AccountMenu` + barra de topo em `page.tsx`

**Files:**
- Create: `src/app/account-menu.tsx`
- Test: `src/app/account-menu.test.tsx`
- Modify: `src/app/page.tsx`
- Modify: `e2e/auth.spec.ts`

A action de signOut entra por **prop** (`signOutAction`) para o componente ser testável em jsdom sem importar módulo `'use server'`.

- [ ] **Step 1: Write the failing test**

Criar `src/app/account-menu.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { AccountMenu } from './account-menu'

describe('AccountMenu (RF-6)', () => {
  it('mostra o e-mail da sessão como gatilho do menu', () => {
    render(
      <AccountMenu email="dono@example.com" theme="dark" signOutAction={async () => {}} />,
    )
    expect(screen.getByText('dono@example.com')).toBeInTheDocument()
  })

  it('contém o toggle de tema e o botão Sair', () => {
    render(
      <AccountMenu email="dono@example.com" theme="dark" signOutAction={async () => {}} />,
    )
    expect(screen.getByRole('button', { name: 'Usar tema claro' })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'Sair' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- --run src/app/account-menu.test.tsx`
Expected: FAIL — `Failed to resolve import "./account-menu"`.

- [ ] **Step 3: Write minimal implementation**

Criar `src/app/account-menu.tsx`:

```tsx
import { ThemeToggle } from './theme-toggle'
import type { Theme } from '@/domain/theme'

/** Menu da conta na barra de topo: e-mail como gatilho, toggle de tema e Sair (RF-6). */
export function AccountMenu({
  email,
  theme,
  signOutAction,
}: {
  email: string
  theme: Theme
  signOutAction: () => Promise<void>
}) {
  return (
    <details className="account-menu">
      <summary>{email}</summary>
      <div>
        <ThemeToggle initialTheme={theme} />
        <form action={signOutAction}>
          <button type="submit">Sair</button>
        </form>
      </div>
    </details>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- --run src/app/account-menu.test.tsx`
Expected: PASS (2 testes).

- [ ] **Step 5: Integrar em `page.tsx`**

Em `src/app/page.tsx`:

1. Acrescentar imports (junto aos existentes; `cookies` vem de `next/headers`, não de `next/server`):

```tsx
import { cookies } from 'next/headers'
import { parseTheme, THEME_COOKIE } from '@/domain/theme'
import { AccountMenu } from './account-menu'
```

2. Dentro de `Home`, logo após obter `user`, resolver o tema:

```tsx
const theme = parseTheme((await cookies()).get(THEME_COOKIE)?.value)
```

3. Substituir o bloco do retorno — de:

```tsx
  return (
    <main style={{ maxWidth: 600, margin: '6vh auto', padding: 24 }}>
      <header style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <h1>MicroTaskManager</h1>
        <form action={signOut}>
          <button type="submit">Sair</button>
        </form>
      </header>
      <p>Sessão de {user?.email ?? 'desconhecido'}.</p>
```

para:

```tsx
  return (
    <>
      <header className="topbar">
        <span className="brand">MicroTaskManager</span>
        <AccountMenu
          email={user?.email ?? 'desconhecido'}
          theme={theme}
          signOutAction={signOut}
        />
      </header>
      <main className="container">
        <h1>Tarefas</h1>
```

4. Trocar o fechamento `</main>` do fim do JSX por:

```tsx
      </main>
    </>
```

O import de `signOut` continua o mesmo (`./login/actions`); só muda o local de uso. O restante do miolo (`ListTabs`, `TaskQuickAdd`, seções) fica intacto dentro do `<main>`.

- [ ] **Step 6: Atualizar `e2e/auth.spec.ts` (Sair agora está dentro do menu)**

De (linhas ~32–35):

```ts
    await expect(page.getByText(EMAIL)).toBeVisible()

    // Logout
    await page.getByRole('button', { name: 'Sair' }).click()
```

para:

```ts
    await expect(page.getByText(EMAIL)).toBeVisible()

    // Logout — o botão Sair fica dentro do menu da conta (GC-17)
    await page.getByText(EMAIL).click()
    await page.getByRole('button', { name: 'Sair' }).click()
```

- [ ] **Step 7: Verificar**

Run: `npm run typecheck && npm run lint && npm test -- --run`
Expected: verde (nenhum teste unitário existente depende do header antigo).

- [ ] **Step 8: Commit**

```bash
git add src/app/account-menu.tsx src/app/account-menu.test.tsx src/app/page.tsx e2e/auth.spec.ts
git commit -m "GC-17 barra de topo + AccountMenu com toggle de tema e Sair"
```

---

## Task 6: Componentes → tokens (PriorityDot monocromático, DueBadge)

**Files:**
- Modify: `src/app/priority-dot.tsx`
- Modify: `src/app/priority-dot.test.tsx`
- Modify: `src/app/due-badge.tsx`

- [ ] **Step 1: Write the failing test (peso visual por prioridade)**

Acrescentar ao `describe` de `src/app/priority-dot.test.tsx`:

```tsx
  it('expõe a prioridade como data-attribute para o peso visual monocromático (GC-17)', () => {
    render(<PriorityDot priority="high" />)
    const dot = screen.getByLabelText('Prioridade: Alta')
    expect(dot).toHaveAttribute('data-priority', 'high')
    expect(dot).toHaveClass('priority-dot')
  })
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- --run src/app/priority-dot.test.tsx`
Expected: FAIL — atributo/classe ausentes.

- [ ] **Step 3: Implementar**

`src/app/priority-dot.tsx` — o `<span>` vira:

```tsx
    <span
      className="priority-dot"
      data-priority={priority}
      aria-label={`Prioridade: ${label}`}
      title={label}
    >
      ●
    </span>
```

(As regras `.priority-dot[data-priority=...]` já existem no `globals.css` da Task 2.)

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- --run src/app/priority-dot.test.tsx`
Expected: PASS (3 testes).

- [ ] **Step 5: DueBadge → tokens (sem mudança de comportamento; testes atuais seguem verdes)**

Em `src/app/due-badge.tsx`, trocar:

```tsx
  const color =
    cls === 'overdue' ? '#e06c75' : cls === 'today' ? '#d19a66' : '#8a8a8a'
```

por:

```tsx
  const color =
    cls === 'overdue'
      ? 'var(--due-overdue)'
      : cls === 'today'
        ? 'var(--due-today)'
        : 'var(--muted)'
```

- [ ] **Step 6: Verificar**

Run: `npm test -- --run src/app/due-badge.test.tsx src/app/priority-dot.test.tsx`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/app/priority-dot.tsx src/app/priority-dot.test.tsx src/app/due-badge.tsx
git commit -m "GC-17 PriorityDot monocromatico por opacidade; DueBadge em tokens"
```

---

## Task 7: Login com tokens

**Files:**
- Modify: `src/app/login/page.tsx`

Sem teste novo: mudança só de apresentação; `e2e/auth.spec.ts` cobre o fluxo.

- [ ] **Step 1: Trocar o inline style do `<main>`**

De:

```tsx
    <main style={{ maxWidth: 360, margin: '10vh auto', padding: 24 }}>
```

para:

```tsx
    <main className="container" style={{ maxWidth: 360, marginTop: '10vh' }}>
```

(Herda tokens/tipografia do body; largura menor específica do login permanece.)

- [ ] **Step 2: Verificar e commitar**

Run: `npm run typecheck && npm run lint && npm test -- --run`
Expected: verde.

```bash
git add src/app/login/page.tsx
git commit -m "GC-17 login consome tokens/container"
```

---

## Task 8: E2E — persistência sem flash (DoD)

**Files:**
- Create: `e2e/theme.spec.ts`

Segue o padrão dos specs existentes (env `E2E_OWNER_*`, skip sem credenciais, cleanup ao final — conta compartilhada entre specs).

- [ ] **Step 1: Criar `e2e/theme.spec.ts`**

```ts
import { test, expect } from '@playwright/test'

const EMAIL = process.env.E2E_OWNER_EMAIL ?? ''
const PASSWORD = process.env.E2E_OWNER_PASSWORD ?? ''

test.describe('Tema (GC-17)', () => {
  test('toggle persiste entre reloads e o SSR já entrega o tema (sem flash)', async ({ page }) => {
    test.skip(!EMAIL || !PASSWORD, 'defina E2E_OWNER_EMAIL/E2E_OWNER_PASSWORD no .env.local')

    // Login (tema padrão: dark — sem data-theme explícito ou data-theme=dark)
    await page.goto('/login')
    await page.fill('input[name="email"]', EMAIL)
    await page.fill('input[name="password"]', PASSWORD)
    await page.getByRole('button', { name: /Entrar/ }).click()
    await expect(page).toHaveURL(/localhost:\d+\/$/)
    await expect(page.locator('html')).not.toHaveAttribute('data-theme', 'light')

    // Abrir menu da conta e mudar para claro → aplica na hora, sem navegação.
    await page.getByText(EMAIL).click()
    await page.getByRole('button', { name: 'Usar tema claro' }).click()
    await expect(page.locator('html')).toHaveAttribute('data-theme', 'light')

    // Reload: persiste via cookie.
    await page.reload()
    await expect(page.locator('html')).toHaveAttribute('data-theme', 'light')

    // Sem flash: o HTML do SERVIDOR já vem com data-theme="light"
    // (page.request compartilha os cookies do contexto).
    const ssrHtml = await (await page.request.get('/')).text()
    expect(ssrHtml).toContain('data-theme="light"')

    // Cleanup: voltar para dark (conta compartilhada entre specs).
    await page.getByText(EMAIL).click()
    await page.getByRole('button', { name: 'Usar tema escuro' }).click()
    await expect(page.locator('html')).toHaveAttribute('data-theme', 'dark')
  })
})
```

- [ ] **Step 2: Rodar os E2E afetados**

Pré-requisito: Supabase local ativo e `.env.local` com `E2E_OWNER_*` (ver `AGENTS.md`).

Run: `npx playwright test e2e/theme.spec.ts e2e/auth.spec.ts`
Expected: PASS (2 specs). Se as credenciais não estiverem definidas, os testes fazem skip — nesse caso reportar como "skip", não como sucesso.

- [ ] **Step 3: Commit**

```bash
git add e2e/theme.spec.ts
git commit -m "GC-17 E2E: tema persiste entre reloads e SSR entrega sem flash"
```

---

## Task 9: Verificação final + PR

- [ ] **Step 1: Gate completo**

Run: `npm run typecheck && npm run lint && npm test -- --run`
Expected: verde.

Run: `npm run e2e`
Expected: todos os specs PASS (auth, tasks, lists, view, theme) com Supabase local + dev server ativos.

- [ ] **Step 2: Push + PR (convenções CLAUDE.md)**

```bash
git push -u origin glaubercosta/gc-17-gc-f-tema-identidade-visual
gh pr create --title "GC-17 GC-f · Tema + identidade visual" --body "Fixes GC-17

- Tokens CSS: dark padrão no :root, light em :root[data-theme=\"light\"]; contraste AA; prefers-reduced-motion.
- Tema persistido em cookie, resolvido no SSR (sem flash), fallback localStorage via script inline.
- Barra de topo + menu da conta (e-mail, toggle de tema, Sair); coluna única ~600px.
- Geist Sans no body; accent violeta parcimonioso; PriorityDot monocromático por opacidade.
- E2E: tema persiste entre reloads e o HTML do servidor já vem com o tema.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

Expected: PR aberto; automação do Linear move GC-17 para **In Progress** e, após merge, **Done** (fecha o épico GC-13 em seguida, manualmente se preciso).

**Não** usar `Ref`/`Part of` para issues concluídas (reabre a issue — lição de GC-5).
